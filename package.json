{
  "name": "cdh-metadata-app",
  "private": true,
  "type": "module",
  "description": "Browser form for authoring CDH metadata records. The app itself has no dependencies; these are for the tests.",
  "scripts": {
    "test": "node test/schema-form.test.mjs && node test/app.test.mjs",
    "drift": "node test/schema-drift.mjs"
  },
  "devDependencies": {
    "jsdom": "^24.0.0",
    "js-yaml": "^4.1.0"
  }
}
